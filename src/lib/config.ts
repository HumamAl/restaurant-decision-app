// App configuration — single source of truth for all identity/attribution text.
export const APP_CONFIG = {
  appName: "DineDecide",
  projectName: "Restaurant Decision App",
  clientName: null as string | null,
  domain: "food",
} as const;
